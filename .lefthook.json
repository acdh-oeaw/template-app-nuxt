{
	"pre-commit": {
		"parallel": false,
		"commands": {
			"oxlint": {
				"glob": "**/*.{js,ts,vue}",
				"run": "pnpm exec oxlint --fix {staged_files}",
				"stage_fixed": true
			},
			"stylelint": {
				"glob": "**/*.{css,vue}",
				"run": "pnpm exec stylelint --cache --fix {staged_files}",
				"stage_fixed": true
			},
			"oxfmt": {
				"run": "pnpm exec oxfmt --no-error-on-unmatched-pattern --write {staged_files}",
				"stage_fixed": true
			}
		}
	}
}
